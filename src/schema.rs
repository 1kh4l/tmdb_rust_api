table! {
    users (id) {
        id -> Int4,
        user_name -> Varchar,
        first_name -> Varchar,
        second_name -> Varchar,
        last_name -> Varchar,
        birth_date -> Varchar,
    }
}
